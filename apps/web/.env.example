# SPA-visible env vars (must be prefixed with VITE_ to reach the browser).
# Copy to .env.local for local dev; configure in Vercel for preview/staging/prod.

VITE_SUPABASE_URL=https://ozvanymuzaqbexchuoxz.supabase.co
VITE_SUPABASE_ANON_KEY=replace-with-anon-key-from-environment.md

VITE_APP_NAME=Team1
VITE_DEFAULT_TENANT_BRAND=team1
VITE_BASE_DOMAIN=team1.app

# Local dev only. In Vercel preview/staging/prod use the deployed URL.
VITE_API_BASE_URL=http://localhost:54321/functions/v1
